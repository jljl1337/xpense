version: "2"
sql:
  - engine: "sqlite"
    schema: "./internal/sql/migration"
    queries: "./internal/sql/query"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        emit_json_tags: true